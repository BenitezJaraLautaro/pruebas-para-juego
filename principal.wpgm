import wollok.game.*
import prota.*


program prubas {
    game.title("prubas juego bar")
    game.height(50) 		
	game.width(50) 			
	game.cellSize(50)
    game.boardGround("fondo2.jpg")	
    game.addVisual(protagonista)
    game.start()
     		
  
}